<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Book Store</title>
    <link rel='stylesheet' href='/stylesheets/style.css'
 />
</head>
<body class="bg-gray-100">
    <header>
        <nav class="bg-gray-800 p-4">
            <div class="container mx-auto flex justify-between items-center">
                <div class="text-white text-2xl font-bold">BookStore</div>
                <div>
    <a href="/" class="text-gray-300 hover:text-white px-4">Home</a>
    <a href="/books" class="text-gray-300 hover:text-white px-4">Books</a>
    
    <% if (user) { %> 
        <span class="text-gray-300 px-4">Welcome, <%= user.username %>!</span> 
        <a href="/logout" class="text-gray-300 hover:text-white px-4">Logout</a> 
    <% } else { %>
        <a href="/register" class="text-gray-300 hover:text-white px-4">Register</a> 
        <a href="/login" class="text-gray-300 hover:text-white px-4">Login</a>
	<a href="/logout" class="text-gray-300 hover:text-white px-4">logout</a>
    <% } %>
</div>
            </div>
        </nav>
    </header>
    <main class="container mx-auto p-4">
        <%- body %>
    </main>
    <footer class="bg-gray-800 text-white text-center p-4 mt-8">
        <p>&copy; 2023 BookStore. All rights reserved.</p>
    </footer>
</body>
</html>
